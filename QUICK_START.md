# 🚀 Быстрый старт RAG ML Handbook

Полная инструкция по настройке и запуску проекта с нуля.

---

## Шаг 1: Создание виртуального окружения

Создайте виртуальное окружение Python с именем `.env`:

```bash
cd /home/dench/Downloads/rag_ml_handbook
python3 -m venv .env
```

> **Важно:** Директория называется именно `.env` (не `venv` и не `.venv`)

---

## Шаг 2: Установка зависимостей

Активируйте виртуальное окружение и установите все необходимые библиотеки:

```bash
# Активация виртуального окружения
source .env/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

Это установит все необходимые библиотеки:
- ✅ langchain и связанные пакеты
- ✅ fastapi и uvicorn
- ✅ python-telegram-bot
- ✅ qdrant-client
- ✅ И другие зависимости

---

## Шаг 3: Создание файла с переменными окружения

Создайте файл `.env.vars` с вашими API ключами и настройками:

```bash
nano .env.vars
# или используйте любой другой текстовый редактор
```

Вставьте следующее содержимое и **замените значения на свои реальные**:

```env
# Токен Hugging Face API
# Получите на: https://huggingface.co/settings/tokens
HUGGINGFACEHUB_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxx

# Токен GROQ API
# Получите на: https://console.groq.com/keys
GROQ_API_TOKEN=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Токен Telegram бота
# Получите у @BotFather в Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ

# URL прокси-сервера (для доступа к GROQ API)
# Формат: http://username:password@host:port
PROXY_URL=http://username:password@196.19.177.20:8000

# Название модели LLM от GROQ
MODEL_NAME=qwen/qwen3-32b

# Название модели для эмбеддингов
EMBEDDER_NAME=BAAI/bge-m3

# URL API сервера (можно оставить по умолчанию)
API_URL=http://localhost:8000
```

Сохраните файл (`Ctrl+O`, `Enter`, `Ctrl+X` в nano).

---

## Шаг 4: Проверка структуры проекта

Убедитесь, что у вас есть:

```bash
ls -la
```

Должны быть:
- ✅ `.env/` - директория с виртуальным окружением
- ✅ `.env.vars` - файл с переменными окружения (ключи)
- ✅ `qdrant_db/` - база данных с векторами
- ✅ `start.sh` - скрипт запуска

---

## Шаг 5: Запуск проекта

Теперь запустите проект одной командой:

```bash
./start.sh
```

Скрипт автоматически:
1. Использует Python из виртуального окружения `.env/bin/python3`
2. Загрузит переменные окружения из `.env.vars`
3. Запустит FastAPI сервер
4. Запустит Telegram бота

Вы увидите вывод:

```
✅ Используется Python: /path/to/.env/bin/python3
Загрузка переменных окружения из .env.vars...

Запуск FastAPI сервера...
Запуск телеграм бота...

✅ API запущен с PID: 12345
✅ Бот запущен с PID: 12346

Для остановки используйте: kill 12345 12346
Или нажмите Ctrl+C
```